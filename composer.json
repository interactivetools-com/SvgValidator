{
    "name": "itools/svgvalidator",
    "description": "Rejects uploaded SVG files that could run script, load external resources, or hang a renderer: an allowlist validator matching what browsers permit for SVG in an <img> tag",
    "keywords": ["php", "svg", "upload", "file-upload", "image-upload", "validator", "validation", "allowlist", "security", "xss", "svg-sanitizer", "sanitize", "xmlreader"],
    "type": "library",
    "homepage": "https://github.com/interactivetools-com/SvgValidator",
    "license": "MIT",
    "readme": "README.md",
    "support": {
        "issues": "https://github.com/interactivetools-com/SvgValidator/issues",
        "source": "https://github.com/interactivetools-com/SvgValidator"
    },
    "authors": [
        {
            "name": "interactivetools.com",
            "homepage": "https://interactivetools.com/"
        }
    ],
    "autoload": {
        "psr-4": {
            "Itools\\SvgValidator\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Itools\\SvgValidator\\Tests\\": "tests/",
            "Itools\\Standards\\": "tests/Integration/"
        }
    },
    "config": {
        "platform": {
            "php": "8.1"
        }
    },
    "extra": {
        "branch-alias": {
            "dev-main": "1.x-dev"
        }
    },
    "require": {
        "php": "^8.1",
        "ext-libxml": "*",
        "ext-xmlreader": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "scripts": {
        "test": "phpunit",
        "coverage": [
            "@putenv XDEBUG_MODE=coverage",
            "phpunit --coverage-text"
        ]
    },
    "scripts-descriptions": {
        "test": "Run the test suite",
        "coverage": "Run the test suite with a line-coverage report (requires Xdebug)"
    }
}
